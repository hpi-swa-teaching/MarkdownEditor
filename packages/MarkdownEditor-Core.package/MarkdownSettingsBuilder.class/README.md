A MarkdownSettingsBuilder is a builder that creates the window for displaying appearances.

Instance Variables
	settings:		<MarkdownSettings>

settings
	- are the associated settings for which a window is created