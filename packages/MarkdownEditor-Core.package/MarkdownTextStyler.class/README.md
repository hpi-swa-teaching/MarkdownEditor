A MarkdownTextStyler styles all blocks of parsed Markdown text.

Instance Variables
	document:		<MarkdownDocument>
	text:			<Text>

document
	- is the parsed Markdown text in tree structure

text
	- is the Markdown text of the editor written by a user
